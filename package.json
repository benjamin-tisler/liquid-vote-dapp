{
  "name": "liquid-vote-hardhat",
  "version": "1.0.0",
  "description": "Hardhat project for LiquidVote and DecisionGraph",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.0",
    "solidity-coverage": "^0.8.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}